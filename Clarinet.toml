[project]
name = "gamma-slippage"
authors = ["Gamma Labs"]
description = "Decentralized fractional asset management protocol"
telemetry = false
requirements = []
[contracts.gamma-slippage]
path = "contracts/gamma-slippage.clar"
depends_on = []

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
